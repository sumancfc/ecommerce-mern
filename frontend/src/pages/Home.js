import React from "react";
import HeroSlider from "../components/slider";
import Layout from "../Layout";

const Home = () => {
  return (
    <Layout>
      <HeroSlider />
    </Layout>
  );
};

export default Home;
